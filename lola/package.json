{
  "name": "lola",
  "private": true,
  "version": "0.1.0",
  "description": "Lola — heritage-language conversation tutor. Get speaking it with your family.",
  "workspaces": [
    "packages/*",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace @lola/shared && npm run build --workspace @lola/server",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace @lola/server",
    "dev:server": "npm run dev --workspace @lola/server"
  },
  "engines": {
    "node": ">=18.18"
  }
}
