# Copy to .env for local development.
# SQLite (default, zero-config):
DATABASE_URL="file:./dev.db"

# Postgres (production / swap target):
# DATABASE_URL="postgresql://user:password@localhost:5432/rscryptofx?schema=public"
